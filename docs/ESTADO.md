# Estado de los tres tracks

Cada track anade una linea al terminar una tarea. Es el unico canal de
comunicacion entre las tres terminales: no hay mensajeria entre sesiones.
Formato: `- [<tarea>] <estado> <fecha> — <nota>`
## Bitacora
- [0.1] hecho — esqueletos y dependencias congeladas
- [0.2] hecho — esquema con calendario academico
- [0.3] hecho — contratos congelados; los tres tracks pueden arrancar
- [A1] hecho 2026-08-20 — POST /api/auth/login y /api/auth/refresh operativos (SecurityConfig, JwtFilter/JwtService en shared/, AuthService+AuthController en user/); se corrigio el hash bcrypt de la semilla V3 (no correspondia a "cambiar123")
- [A2] hecho 2026-08-20 — GET/PUT /api/calendar/school-days e isSchoolDay operativos (paquete calendar/ con controller, service, repository, model)
- [Cierre-Task2] hecho 2026-08-20 — Home.tsx: el docente ya ve el enlace a Consultas y coordinacion/admin ven ademas el enlace al Tablero (antes solo se llegaba escribiendo la URL); `npm run build` OK. Verificacion manual de los dos roles (Step 3) no ejecutada por no tener backend+dev server levantados en esta sesion.
- [V2-Task1..3] hecho 2026-08-21 — el QR del carnet trae el texto completo ("Alvaro Mathias Orozco Lara 1013696566 Primero - 103"), no el numero suelto. `parseCarnet` en `scan/carnet.ts` (6 tests) y `CarnetParser` en el backend (2 tests): el parseo va en los dos lados porque la cola offline puede llevar escaneos viejos con el texto crudo, y arreglar solo el navegador los dejaria rechazados para siempre. `Ingreso.tsx` avisa si el curso del carnet no coincide con el de la base (4 tests). Manda la base, nunca el carnet.
- [V2-Task4..6] hecho 2026-08-21 — tres informes en Excel como tipos del endpoint que ya existia (`?tipo=matriz|inasistencias|individual`); sin `tipo` sigue devolviendo el resumen de 9 columnas que comprueba `humo.sh`. `ExcelTest` con 10 casos que miran el contenido del libro, no solo el HTTP 200. En la matriz, celda vacia = sin registro, no falta. El individual traduce P/T/F/E a texto legible porque va a manos del acudiente, y exige `studentId` (400 sin el, 403 para ACUDIENTE).
- [V2-Task7] hecho 2026-08-21 — `Consultas.tsx` gana el selector de informe (3 tests). El individual no se expone ahi: se pedira desde la ficha del estudiante, que aun no existe.
- [V2-Task8] hecho 2026-08-21 — `PUT /api/calendar/school-days` marca un rango completo saltando fines de semana, y el panel gana el formulario con el aviso de cuantos dias cambio. El test comprueba ademas que la cache en memoria de dias lectivos se entera: sin eso el servidor seguiria aceptando asistencia en un dia ya suspendido. 8 tests backend + 2 frontend.
- [V2-Task9] hecho 2026-08-21 — test de regresion del aislamiento del portal del acudiente, con dos acudientes de hijos distintos (con uno solo no se distingue "ve a los suyos" de "ve a todos"). Verificado al reves: al anadir a proposito un `studentId` al controlador, el test se cae. Se deshizo.
- [V2-Task10..11] hecho 2026-08-21 — despliegue Vercel + Supabase + Fly.io: `apiUrl()` con `VITE_API_URL`, CORS por `APP_CORS_ORIGINS`, `vercel.json`, `fly.toml` y la Opcion C en `docs/DESPLIEGUE.md` (usar el Session pooler 5432 de Supabase, no el Transaction pooler 6543). Un test guardian falla si alguien vuelve a usar `fetch('/api/...')` relativo: ya cazo uno en `PanelCarga.tsx`.
- [V2-Task12] PARCIAL 2026-08-21 — verificado: 77/77 backend en orden aleatorio, 67/67 frontend, build limpio (98.05 KB gzip), `humo.sh` con sus 16 invariantes en verde contra la aplicacion corriendo, los tres Excel comprobados celda a celda contra la API viva y el carnet con texto completo aceptado (`accepted:1`). **PENDIENTE: el recorrido en navegador real no se ejecuto porque la extension de Chrome no estaba conectada.** Tampoco se ha desplegado en Vercel/Supabase/Fly: la configuracion esta escrita y validada sintacticamente, pero sin desplegar es una hipotesis, igual que lo fue el Dockerfile en la cuarta iteracion.

## Cambios al contrato (requiere acuerdo de los tres)
_(vacio)_
## Bloqueos
- [A3] hecho — /api/sync/bootstrap incluye schoolDays; contrato verificado
- [A4] hecho — sync valida dia lectivo; TRACK A COMPLETO
- [B1] hecho 2026-08-20 — cliente HTTP, login y modo mock (VITE_MOCK=1)
- [B2] hecho 2026-08-20 — IndexedDB con calendario; markAttendance bloquea dias no lectivos
- [B3] hecho 2026-08-20 — pantalla de asistencia con selector de fecha y bloqueo de no lectivos
- [B4] hecho 2026-08-20 — PWA instalable (manifest, iconos, service worker, BotonInstalar) y pantalla de ingreso por escaneo de carnet (BarcodeDetector nativo con respaldo @zxing/browser diferido); bundle inicial 92.3 KB gzip. TRACK B COMPLETO
- [C2] hecho 2026-08-20 — dashboard con KPIs, barras por curso y tendencia diaria en SVG (geometria.ts + 5 tests de vitest en verde). `npm run build` falla por ahora con "Module has no exported member 'api'" en `frontend/src/api/client.ts`: ese fichero es de Track B (Task B1) y hoy es solo el stub de la Fase 0; no se toco por ser propiedad ajena. Se resuelve solo al fusionar con track-b-app.
- [C1] hecho 2026-08-20 — ingreso por carnet (`POST /api/entry/sync`), reportes (`/api/reports/summary`, `/excel`, `/pending-today`) y `/api/reports/dashboard`, con `schoolDays` en el resumen y columna "Dias lectivos" en el Excel. Para compilar hizo falta traer del Track A el `calendar/` (Task A2 completa: DayType, SchoolDay, CalendarRepository/Service/Controller + CalendarTest) y crear `student/model/Student.java` + `student/repository/StudentRepository.java` (parte de la Task A3 que aun no existia): sin ellos ni `CalendarService.isSchoolDay` ni `EntryController`/`ReportRepository` compilaban. Se fusiono localmente `track-a-nucleo` (solo tenia A1) para no reescribir JwtService a mano. Track A: al retomar A2/A3 revisar que coincida con lo ya creado aqui (paquetes `calendar/` y `student/`) para fusionar sin duplicar. 23/23 tests backend en verde (`mvn test` contra `asistencia_test_c`).
- [C3] hecho — cola de notificaciones (V20) con envio cada 15 minutos
- [C4] hecho 2026-08-20 — consultas con dias lectivos, portal del acudiente e importacion CSV; TRACK C COMPLETO. Anadidos `GuardianController` (`/api/guardian/children`, solo ve sus propios hijos via `guardianships`, sin aceptar `studentId` por parametro) e `ImportController` (`POST /api/admin/import/students`, upsert por `document_id`), con `findChildren`/`recentAttendance`/`upsert` en `StudentRepository`; no dependen de `AttendanceRepository`/`ScheduleRepository` de v1 porque esos paquetes son de otro track y no existen aun en este worktree, se consulta `attendance`/`schedule_blocks`/`subjects` con SQL nativo igual que hace `ReportRepository`. `Consultas.tsx` y `Padre.tsx` ya usan `api`/`getSession` como `Dashboard.tsx` (Task C2): `npm run build` sigue fallando por el mismo motivo ya anotado en [C2] (stub de `frontend/src/api/client.ts`, propiedad de Track B); `npm test` pasa. Se omitio el banner de bloques pendientes en `Home.tsx` (Task 11 Step 10 de v1) porque ese fichero es propiedad de Track B segun la tabla de propiedad. 33/33 tests backend en verde (`mvn test` contra `asistencia_test_c`).
- [D1] hecho 2026-08-20 — cierre Task 1 del plan de cierre-y-produccion: `dominioY` ensancha 5 puntos a cada lado cuando el rango es plano (un solo dia o valores iguales), sin salirse de 0-100; el grafico de tendencia ya no muestra las dos etiquetas iguales con un solo dato. `npm test` en `app/frontend`: 19/19 en verde (hizo falta `npm install`, no habia `node_modules`).
- [Cierre-Task3] hecho 2026-08-21 — `SpaConfig` (`shared/config`) reenvia rutas sin punto que no empiecen por `/api` a `/index.html`, para que recargar `/asistencia` o `/dashboard` en produccion no de 404. Hizo falta ademas permitir en `SecurityConfig` el acceso sin token a todo lo que no sea `/api/**` (regex `^(?!/api/).*$`), porque antes `anyRequest().authenticated()` devolvia 401 antes de llegar al reenvio: la SPA y sus estaticos son publicos por diseno, la proteccion real esta en los endpoints de `/api`. 45/45 tests backend en verde contra `asistencia_test_a`.
- [Cierre-Task4] hecho 2026-08-21 — `Dockerfile` de tres etapas (frontend node:22-alpine -> backend maven:3.9-eclipse-temurin-21 -> imagen final eclipse-temurin:21-jre-alpine con `asistencia-0.1.0.jar`), `.dockerignore` y `.github/workflows/ci.yml` (jobs `backend` con Postgres 16 de servicio, `frontend`, `imagen` que depende de los dos). YAML validado con `python -c "import yaml; ..."` → `YAML OK`. **No se pudo construir la imagen en esta maquina: no hay Docker instalado** (`docker --version` da "command not found"). Queda pendiente de que el job `imagen` de la CI la construya en el primer push, o que alguien con Docker local la valide antes: mientras tanto el Dockerfile es una hipotesis razonable, no un entregable verificado.
- [Cierre-Task5] hecho 2026-08-21 — `ImportService` (nuevo, en `service/`) con `importarHorario`/`importarAcudientes`; crea materia/docente/acudiente que falten (docente y acudiente con contrasena temporal `cambiar123`, sin sobreescribirla si ya existen), reimportar el mismo horario actualiza por `ON CONFLICT` sin duplicar, y una linea mal formada se reporta sin abortar el resto (cada linea en su propia transaccion, `Propagation.NOT_SUPPORTED`). `ImportController` gana `POST /api/admin/import/schedule` y `/guardians` (ADMIN, ya cubierto por el `@PreAuthorize` de la clase). Migracion `V30__indices_importacion.sql` (indices en `guardianships.guardian_id` y `users.email` para activos). `AbstractIntegrationTest` gana el helper `tokenDe(email, rol)` para no repetirlo en cada test. `app/README.md` documenta los tres CSV y el orden de carga (estudiantes -> horario -> acudientes). 50/50 tests backend en verde contra `asistencia_test_a` (`TEST_DB_URL=jdbc:postgresql://localhost:5432/asistencia_test_a`).
- [Admin-Task1] hecho 2026-08-21 — API de administracion de usuarios: `AdminUserService` (`service/`) y `AdminUserController` (`controller/`, `@PreAuthorize("hasRole('ADMIN')")`) con `GET/POST /api/admin/users`, `PUT /api/admin/users/{id}` y `POST /api/admin/users/{id}/reset-password`; las bajas son logicas (`active=false`) para no romper la trazabilidad de `recorded_by`. `UserRepository` gana `findByRoleOrderByFullName`, `findAllByOrderByFullName` y `existsByEmail`. 56/56 tests backend en verde contra `asistencia_test_b`, en orden alfabetico y reverso (`AdminUserTest` no muta la semilla, usa correos `*.admintest.co`).
- [Admin-Task2] hecho 2026-08-21 — pantalla `/admin` (solo ADMIN) con tres pestanas: `PanelUsuarios` (alta, listado con filtro/busqueda, desactivar/activar, restablecer clave), `PanelCalendario` (cambiar tipo de dia y motivo, PUT en onChange/onBlur) y `PanelCarga` (subida de CSV de estudiantes/horario/acudientes con reporte de errores por linea). `AdminUser`/`ImportResult` anadidos a `contract.ts`; enlace "Administracion" en `Home.tsx` para rol ADMIN. Al aplicar el codigo del plan aparecieron dos huecos que no estaban cubiertos por ningun track: `api/client.ts` no tenia metodo `put` (solo `get`/`post`) y se anadio siguiendo el mismo patron; `PanelCarga.tsx` tal como esta en el plan usa `await` dentro de un callback no-async de `setResultado`, error de compilacion TS1308, se resolvio esperando el `await` antes del `setResultado`. `npm run build` limpio (chunks 97.4/107.7 KB gzip, bajo el limite de 200 KB) y `npm test`: 19/19 en verde. Verificacion manual de los tres paneles (Step 8) no ejecutada: no hay backend+dev server levantados en esta sesion.
- [Tests-Interfaz-Task1] hecho 2026-08-21 — entorno de tests de componentes: `@testing-library/react`/`jest-dom`/`user-event` como devDependencies, `src/test-setup.ts` (cleanup + unstubAllGlobals + restoreAllMocks tras cada test) registrado en `vite.config.ts` (`setupFiles`, `globals: true`). Tests nuevos: `BannerEstado.test.tsx` (4 casos: vacio con conexion sin pendientes, aviso sin conexion, sin boton sin conexion, enviar con pendientes) y `SelectorFecha.test.tsx` (5 casos: dia lectivo sin aviso, festivo con motivo, suspendido, fecha fuera de calendario descargado, atajos solo con dias LECTIVO). Ningun componente necesito cambios: `SelectorFecha` ya filtraba los atajos por `dayType === 'LECTIVO'`. `npm test`: 28/28 en verde (19 previos + 9 nuevos). `npm run build`: chunk principal sigue en 97.44 KB gzip, sin crecer.
- [Tests-Interfaz-Task2] hecho 2026-08-21 — `TomarAsistencia.test.tsx` (4 casos: sin curso no hay estudiantes, filtro por curso, marcar falta la deja en `db.outbox` sin llamar a `fetch`, el selector de curso solo ofrece los cursos del horario del docente) y `PanelCalendario.test.tsx` (3 casos: lista dias del rango, PUT correcto al cambiar tipo de dia, mensaje de error si el PUT falla). Ningun componente necesito cambios: `TomarAsistencia` ya tenia `role="group"`/`aria-label` en el bloque de estados. Los dos primeros tests de `TomarAsistencia` fallaban por una condicion de carrera del propio test (el `<select>` de curso ya esta en el DOM antes de que `db.blocks.toArray()` resuelva y llene las opciones); se ajusto el `waitFor` para esperar a que haya mas de la opcion "Seleccione..." en vez de solo la presencia del `<select>`. `npm test`: 35/35 en verde (28 previos + 7 nuevos). `npm run build`: chunk principal sigue en 97.44 KB gzip.
- [Tests-Restantes-Task1] hecho 2026-08-21 — `Padre.test.tsx` (7 casos: nombre y curso del hijo, estados traducidos a texto legible, no lista dias con asistencia normal, muestra comentario del docente, cuenta novedades y no registros totales, mensaje de error sin conexion, acudiente sin hijos no rompe la pantalla). El componente ya cumplia todo lo que pedia el plan (contador de novedades correcto, `role="alert"` con el texto esperado): los siete tests pasaron sin tocar `Padre.tsx`. `npm test src/pages/Padre.test.tsx`: 7/7 en verde.
- [Tests-Restantes-Task2] hecho 2026-08-21 — `PanelUsuarios.test.tsx` (4 casos: lista usuarios con rol/estado, aviso con contrasena temporal al crear, correo duplicado avisa en vez de fallar en silencio, desactivar manda PUT con `active:false`) y `PanelCarga.test.tsx` (4 casos: cabecera de las tres cargas, filas importadas, detalle de lineas con error, aviso si el servidor rechaza el archivo). Ninguno de los dos componentes necesito cambios: ya mostraban el aviso de contrasena temporal, el error 409 y el detalle de errores por linea tal como pedia el plan. `npm test`: 50/50 en verde (35 previos + 7 de `Padre` + 8 nuevos). `npm run build`: chunk principal en 97.45 KB gzip, bajo el limite de 200 KB.
- [Diseno-Task1..5] hecho 2026-08-21 — direccion visual "Planilla" (papel crema, filete de margen rojo, casillas de estado como elemento firma) en `styles.css`, con Archivo variable autoalojada (`public/fonts/archivo-latin.woff2`, 34 KB, subconjunto latino, precacheada por el SW y con `rel=preload`): el cuerpo sigue en la pila del sistema, 0 KB. Los colores de `ESTADOS` en `contract.ts` se alinean con la paleta y la evasion pasa de naranja a violeta porque en la barra apilada era indistinguible de la falta. `Home` cambia los cinco botones verdes iguales por fichas con una linea de contexto cada una. El tablero saca los filtros de `.leyenda` (que era la clase de la leyenda de graficas) a `.filtros-tablero`, jerarquiza la tasa de asistencia como KPI principal y marca las alertas con el filete del margen en vez de tenir la cifra de rojo, que al sol pierde contraste. Seguridad: `/asistencia` e `/ingreso` pasan de `Protegida` a `SoloRoles roles={PERSONAL}`; antes un ACUDIENTE con sesion valida entraba escribiendo la URL y veia el curso completo antes de que fallara la peticion. `App.test.tsx` (4 casos) lo fija. 71/71 tests frontend, build 98.63 KB gzip. Verificacion visual: capturas de las cuatro pantallas con Chrome en headless y medicion del ancho de scroll (390 px, sin desbordes) — **la extension de Chrome sigue sin conectarse**, asi que el recorrido interactivo (V2-Task12) continua pendiente.
- [Pendientes-Hoy] hecho 2026-08-21 — el inicio del docente muestra los bloques que hoy siguen sin asistencia (`GET /api/reports/pending-today`, que existia desde C1 pero no estaba en ninguna pantalla) y cada uno enlaza a `/asistencia?grade=..&blockId=..` con el curso y el bloque ya elegidos, para no volver a escogerlos a mano. Sin conexion el aviso simplemente no aparece: el calculo es del servidor y la pantalla tiene que seguir usable. `Home.test.tsx` (3 casos) y `MemoryRouter` en los tests de `TomarAsistencia`, que ahora usa `useSearchParams`. 74/74 frontend, build 99.06 KB gzip.
